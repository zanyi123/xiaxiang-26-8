# 江门侨乡建筑数字化云游平台 - 完整部署教程

> 本文档包含从零开始部署到腾讯云香港服务器的完整流程

---

## 目录

1. [腾讯云COS配置](#1-腾讯云cos配置)
2. [域名配置](#2-域名配置)
3. [服务器配置](#3-服务器配置)
4. [项目打包](#4-项目打包)
5. [服务器环境搭建](#5-服务器环境搭建)
6. [上传项目到服务器](#6-上传项目到服务器)
7. [运行项目](#7-运行项目)
8. [配置Nginx反向代理](#8-配置nginx反向代理)
9. [后台运行项目](#9-后台运行项目)
10. [最终验证](#10-最终验证)
11. [日常维护](#11-日常维护)

---

## 1. 腾讯云COS配置

### 1.1 存储桶信息

| 配置项 | 值 |
|--------|-----|
| 存储桶名称 | `qiaoyun-2026-gdut-100050675333-1453696876` |
| 地域 | `ap-guangzhou`（华南地区广州） |
| 访问域名 | `https://qiaoyun-2026-gdut-100050675333-1453696876.cos.ap-guangzhou.myqcloud.com/` |

### 1.2 配置存储桶权限（公有读私有写）

1. 打开：`https://console.cloud.tencent.com/cos/bucket`
2. 点击存储桶 `qiaoyun-2026-gdut-100050675333-1453696876`
3. 左侧菜单 → **权限管理** → **访问控制列表(ACL)**
4. 选择 **"公有读私有写"**
5. 保存

### 1.3 获取API密钥

1. 打开：`https://console.cloud.tencent.com/cam/capi`
2. 点击 **"新建密钥"**
3. 复制保存：
   - `SecretId`（类似 `AKIDxxxxxxxx`）
   - `SecretKey`（32位字符串）
4. ⚠️ **SecretKey 只显示一次，必须立即保存！**

### 1.4 填写项目配置

打开 `d:\JAVA\xiaxiang\src\main\resources\application.yml`，填写：

```yaml
cos:
  secret-id: "你的SecretId"
  secret-key: "你的SecretKey"
  region: "ap-guangzhou"
  bucket-name: "qiaoyun-2026-gdut-100050675333-1453696876"

app:
  mock-mode: false  # 关闭Mock模式，使用真实COS
```

### 1.5 上传素材到COS

等素材准备好后：

1. 打开：`https://console.cloud.tencent.com/cos/bucket`
2. 点击存储桶 → 文件列表 → 上传文件
3. 按照以下结构上传：

```
qiaoyun-2026-gdut-100050675333-1453696876/
├── cover-kaiping.jpg        （封面图，直接传到根目录）
├── cover-zili.jpg
├── cover-chikan.jpg
├── models/                  （创建文件夹）
│   ├── kaiping.splat        （3D模型文件）
│   ├── zili.splat
│   └── chikan.splat
└── videos/                  （创建文件夹）
    ├── kaiping-4k.mp4       （视频文件）
    ├── zili-4k.mp4
    └── chikan-4k.mp4
```

---

## 2. 域名配置

### 2.1 域名信息

| 配置项 | 值 |
|--------|-----|
| 域名 | `xinhuoqiaoyun.online` |
| 域名服务商 | DNSPod（腾讯云） |

### 2.2 域名实名认证

1. 打开：`https://console.cloud.tencent.com/domain`
2. 找到域名 `xinhuoqiaoyun.online`
3. 点击 **"实名认证"**
4. 选择 **"个人认证"**
5. 填写身份证信息并上传照片
6. 等待审核（1-3个工作日）

> ⚠️ 香港服务器不需要ICP备案，但域名实名认证是必须的！

### 2.3 配置DNS解析

1. 打开：`https://console.dnspod.cn/dns/list`
2. 点击域名 `xinhuoqiaoyun.online` → **"解析"**
3. 添加记录：

| 记录类型 | 主机记录 | 记录值 | TTL |
|----------|---------|--------|-----|
| A | @ | 你的服务器IP | 600 |
| A | www | 你的服务器IP | 600 |

4. 等待10分钟生效

---

## 3. 服务器配置

### 3.1 服务器信息

| 配置项 | 值 |
|--------|-----|
| 服务商 | 腾讯云 |
| 地区 | 香港 |
| 类型 | 轻量应用服务器 |
| 操作系统 | Ubuntu 20.04 |
| 公网IP | （填写你的实际IP） |

### 3.2 重置密码（如果忘记密码）

1. 打开：`https://console.cloud.tencent.com/lighthouse/instance`
2. 找到你的香港服务器
3. 点击右侧 **"更多"** → **"重置密码"**
4. 输入新密码（如 `Xiaxiang2026!`）
5. 勾选 **"强制关机"**
6. 点击"确定"
7. 等待1分钟重启完成

### 3.3 连接服务器

**Windows PowerShell连接**：
```powershell
ssh ubuntu@你的IP地址
# 输入密码
```

> 如果使用密钥登录，命令为：
> ```powershell
> ssh -i 私钥文件路径 ubuntu@你的IP地址
> ```

---

## 4. 项目打包

### 4.1 Maven打包

1. 在IDE中打开右侧 **Maven** 工具栏
2. 展开 `xiaxiang-building-tour` → `Lifecycle`
3. 双击 **"package"**
4. 等待底部显示 `BUILD SUCCESS`

### 4.2 验证打包结果

打包完成后，在以下路径找到jar文件：
```
d:\JAVA\xiaxiang\target\xiaxiang-building-tour-1.0-SNAPSHOT.jar
```

---

## 5. 服务器环境搭建

SSH连接上服务器后，依次执行以下命令：

### 5.1 更新系统

```bash
sudo apt-get update
```

### 5.2 安装Java 11

```bash
sudo apt-get install -y openjdk-11-jdk
```

### 5.3 验证Java安装

```bash
java -version
# 应该显示 openjdk version "11.0.x"
```

### 5.4 创建项目目录

```bash
mkdir -p /opt/xiaxiang
cd /opt/xiaxiang
```

---

## 6. 上传项目到服务器

### 方法一：SCP命令（在本地PowerShell执行）

```powershell
scp d:\JAVA\xiaxiang\target\xiaxiang-building-tour-1.0-SNAPSHOT.jar ubuntu@你的IP地址:/opt/xiaxiang/
```

### 方法二：WinSCP工具（图形化，推荐新手）

1. 下载 WinSCP：`https://winscp.net/eng/download.php`
2. 新建会话：
   - 文件协议：SFTP
   - 主机名：你的服务器IP
   - 用户名：`ubuntu`
   - 密码：你的密码
3. 连接后，把jar包拖到 `/opt/xiaxiang/` 目录

---

## 7. 运行项目

### 7.1 前台运行（测试用）

```bash
cd /opt/xiaxiang
java -jar xiaxiang-building-tour-1.0-SNAPSHOT.jar
```

看到以下日志表示启动成功：
```
[COS] COSClient 初始化完成
Started App in x.xxx seconds
```

### 7.2 测试访问

- 浏览器访问：`http://你的IP地址:8080`
- 或域名访问：`http://xinhuoqiaoyun.online:8080`

---

## 8. 配置Nginx反向代理

让域名直接访问（去掉 `:8080`）。

### 8.1 安装Nginx

```bash
sudo apt-get install -y nginx
```

### 8.2 创建配置文件

```bash
sudo nano /etc/nginx/sites-available/xiaxiang
```

粘贴以下内容：

```nginx
server {
    listen 80;
    server_name xinhuoqiaoyun.online;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

按 `Ctrl+O` 保存，`Enter` 确认，`Ctrl+X` 退出。

### 8.3 启用配置

```bash
sudo ln -s /etc/nginx/sites-available/xiaxiang /etc/nginx/sites-enabled/
```

### 8.4 测试配置

```bash
sudo nginx -t
# 应该显示 "test is successful"
```

### 8.5 重启Nginx

```bash
sudo systemctl restart nginx
```

---

## 9. 后台运行项目

让项目在关闭SSH后继续运行。

### 9.1 安装screen

```bash
sudo apt-get install -y screen
```

### 9.2 创建新会话

```bash
screen -S xiaxiang
```

### 9.3 运行项目

```bash
cd /opt/xiaxiang
java -jar xiaxiang-building-tour-1.0-SNAPSHOT.jar
```

### 9.4 退出会话（项目继续运行）

1. 按 `Ctrl+A`
2. 松开后按 `D`
3. 显示 `[detached]` 表示成功退出

### 9.5 重新进入会话（需要查看日志时）

```bash
screen -r xiaxiang
```

---

## 10. 最终验证

### 10.1 访问测试

| 地址 | 预期结果 |
|------|----------|
| `http://xinhuoqiaoyun.online` | ✅ 显示项目首页 |
| `http://www.xinhuoqiaoyun.online` | ✅ 显示项目首页 |
| `http://你的IP地址` | ✅ 显示项目首页 |

### 10.2 完整链路

```
用户访问域名
    ↓
DNS解析到服务器IP
    ↓
Nginx接收请求(80端口)
    ↓
反向代理到Java项目(8080端口)
    ↓
Java项目返回页面
    ↓
页面加载COS中的素材(3D模型/视频/图片)
    ↓
用户看到完整页面
```

---

## 11. 日常维护

### 11.1 查看项目日志

```bash
tail -f /opt/xiaxiang/app.log
# Ctrl+C 退出
```

### 11.2 更新项目（标准流程）

**Step 1：本地打包**（在IDEA或本地终端）
```
Maven → xiaxiang-building-tour → Lifecycle → package
```
生成：`d:\JAVA\xiaxiang\target\xiaxiang-building-tour-1.0-SNAPSHOT.jar`

**Step 2：WinSCP上传**
1. 连接服务器（SFTP）
2. 本地 `target/` 目录的 jar 拖到服务器 `/opt/xiaxiang/`
3. 弹窗选"是"覆盖旧文件

**Step 3：OrcaTerm 重启**
```bash
cd /opt/xiaxiang
sudo pkill -9 -f xiaxiang 2>/dev/null; sleep 3
nohup java -jar xiaxiang-building-tour-1.0-SNAPSHOT.jar --app.env=prod > app.log 2>&1 &
echo $! > app.pid
sleep 5 && tail -5 app.log
```
看到 `Started App` 就成功了，浏览器访问 `http://xinhuoqiaoyun.online` 验证。

### 11.3 服务器配置文件（一次性创建，不要上传覆盖）

`/opt/xiaxiang/application-local.yml`（COS密钥 + env: prod）：
```yaml
cos:
  secret-id: "你的SecretId"
  secret-key: "你的SecretKey"
  region: "ap-guangzhou"
  bucket-name: "qiaoyun-2026-gdut-100050675333-1453696876"

app:
  mock-mode: false
  env: prod
```
⚠️ **此文件仅在服务器上手动创建，不要用WinSCP上传覆盖！**

### 11.4 常用命令速查

| 命令 | 作用 |
|------|------|
| `tail -f /opt/xiaxiang/app.log` | 查看实时日志 |
| `cat /opt/xiaxiang/app.pid` | 查看当前进程PID |
| `sudo kill -9 $(cat /opt/xiaxiang/app.pid)` | 杀掉旧进程 |
| `sudo netstat -tlnp \| grep :8080` | 检查8080端口 |
| `sudo /www/server/nginx/sbin/nginx` | 启动Nginx（宝塔） |
| `sudo /www/server/nginx/sbin/nginx -t` | 测试Nginx配置 |
| `sudo killall -9 nginx` | 杀掉所有Nginx进程 |

---

## 附录：快速检查清单

### 配置检查

| 序号 | 步骤 | 是否完成 |
|------|------|----------|
| 1 | 存储桶权限配置（公有读私有写） | ☐ |
| 2 | 获取API密钥（SecretId/SecretKey） | ☐ |
| 3 | 填写application.yml配置 | ☐ |
| 4 | 关闭Mock模式 | ☐ |
| 5 | 域名实名认证 | ☐ |
| 6 | DNS解析配置 | ☐ |
| 7 | 服务器密码重置 | ☐ |
| 8 | SSH连接服务器 | ☐ |
| 9 | 安装Java环境 | ☐ |
| 10 | Maven打包项目 | ☐ |
| 11 | 上传jar包到服务器 | ☐ |
| 12 | 前台运行测试 | ☐ |
| 13 | 配置Nginx | ☐ |
| 14 | 后台运行项目 | ☐ |
| 15 | 域名访问验证 | ☐ |

---

## 附录：安全提醒

1. **API密钥安全**：`secret-id` 和 `secret-key` 绝不提交到Git
2. **服务器密码**：设置复杂密码，定期更换
3. **SSH密钥**：推荐使用密钥登录，比密码更安全
4. **防火墙**：只开放需要的端口（80、443、22）
5. **定期备份**：重要数据定期备份

---

## 附录：故障排查手册

### 问题1：WinSCP连接服务器超时（网络错误）

#### 症状
```
错误：网络错误：连接 "IP地址" 超时
```

#### 排查步骤

**Step 1：检查服务器IP是否变化**
```bash
# 在OrcaTerm/SSH终端执行，获取当前公网IP
curl ifconfig.me
# 或
hostname -I
```
如果IP和之前不一样，更新WinSCP的主机名配置。

**Step 2：检查网络连通性**
```powershell
# Windows命令提示符（Win+R → cmd）
ping 服务器IP
```
- ✅ 有回复 → 网络通，检查WinSCP配置
- ❌ 请求超时 → 服务器可能关机/IP变了

**Step 3：WinSCP重新连接**
1. 打开WinSCP → 新建站点
2. 文件协议：`SFTP`
3. 主机名：`服务器公网IP`
4. 端口：`22`
5. 用户名：`ubuntu`
6. 密码：你的密码
7. 点【保存】→【登录】

**Step 4：服务器目录导航**
连接成功后，在右侧面板地址栏输入：
```
/opt/xiaxiang
```
按回车即可跳转到项目目录。

---

### 问题2：Maven打包报错 `error in opening zip file`

#### 症状
```
[ERROR] Failed to execute goal org.springframework.boot:spring-boot-maven-plugin:2.7.18:repackage
[ERROR] Error reading archive file: error in opening zip file
```

#### 原因
旧的jar文件被占用（本地仍有Java进程在运行），或文件损坏。

#### 解决方案

**方法1：命令行打包（最稳）**
```powershell
# 1. 先停掉本地运行的Spring Boot程序（IDEA点红色方块）
# 2. 打开cmd（Win+R → cmd）
# 3. 进入项目目录
cd /d d:\JAVA\xiaxiang

# 4. 清理旧的target目录
rmdir /s /q target

# 5. 重新打包（跳过测试加快速度）
mvn clean package -DskipTests
```

**方法2：IDEA内打包**
1. 停掉本地运行的Spring Boot程序
2. 菜单 `Build` → `Rebuild Project`
3. 或Maven面板：双击 `clean` → 双击 `package`

---

### 问题3：WinSCP界面异常（找不到菜单/搜索无反应）

#### 症状
- 右侧显示本地Windows文件（C:\盘），不是远程Linux
- 工具栏/菜单消失

#### 解决方案
1. **按 `F11`** 切换全屏模式，恢复菜单栏
2. **按 `Ctrl+Alt+S`** 打开站点管理器
3. 如果仍不行，**关闭WinSCP重开**（最直接）
   - `Alt+F4` 关闭
   - 重新打开后会自动弹出登录窗口
   - 选择服务器连接 → 登录

---

### 问题4：服务器目录 `/opt/xiaxiang` 不存在

#### 排查
```bash
# SSH连接服务器后执行
ls /opt/
# 如果/opt是空的
sudo mkdir -p /opt/xiaxiang
sudo chown ubuntu:ubuntu /opt/xiaxiang
```

#### 说明
- Linux的 `/opt` 根目录，不在任何"盘"里
- 用WinSCP时，在地址栏直接输入 `/opt/xiaxiang` 回车
- 或双击右侧面板的 `..` 逐级向上导航到根目录 `/`

---

## 附录：联系方式

如有问题，请检查：
1. 项目日志：`screen -r xiaxiang`
2. Nginx日志：`sudo tail -f /var/log/nginx/error.log`
3. 服务器状态：`sudo systemctl status nginx`
